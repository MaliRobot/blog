name: Nonliteral Tests

on:
  pull_request:
    branches:
      - master

jobs:
  test:
    name: Nonliteral Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.12

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt  # Adjust based on how you manage dependencies

      - name: Run Django tests
        run: |
          cd blog
          python manage.py test